@model List<Role>

<div class="container">
    <div class="row">
        <div class="col-lg-12">
            <table class="table table-striped">
                <thead>
                    <tr>
                        <th scope="col">#</th>
                        <th scope="col">Role Name</th>
                        <th scope="col">Manipulation</th>
                    </tr>
                </thead>
                <tbody>
                    @for (int i = 0; i < Model.Count; i++)
                    {
                        <tr>
                            <th scope="row">@(i+1)</th>
                            <td>@(Model[i].Name)</td>
                            <td>
                                <div class="d-flex align-items-center" style="column-gap: 4px;">
                                    <a href="@(RouteConstant.ADMIN_DOMAIN)?roleID=@(Model[i].Id)&roleName=@(Model[i].Name)" class="btn btn-warning btn-sm" ><i class="bi bi-eye"></i></a>
                                    <button class="btn btn-danger btn-sm btnDeleteBook" data-RoleID="@(Model[i].Id)"><i class="bi bi-trash"></i></button>
                                    <a href="@(RouteConstant.ADMIN_DOMAIN_MODIFY)?menuKey=RM&RoleID=@(Model[i].Id)" class="btn btn-info btn-sm text-white"><i class="bi bi-pencil-square"></i></a>
                                </div>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>